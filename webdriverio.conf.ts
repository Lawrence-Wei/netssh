/**
 * Compatibility WDIO config.
 *
 * Older scripts used this filename. Keep it aligned with wdio.conf.ts so every
 * E2E entry point runs the current src/test/e2e suite.
 */

export { config } from "./wdio.conf";
